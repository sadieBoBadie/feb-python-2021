// Counting, the Dojo Way - Print integers 1 to 100. 
// If divisible by 5, print "Coding" instead. If divisible 
// by 10, print "Coding Dojo".

/*
    (no function necessary, no variables)
    Create a loop, 
    set i to 1, 
    keep going while i is <= 100, 
    increment i by 1
        if i is divisible by 10 (using modulus?)
            print "Coding Dojo"
        else if i disivible by 5 -- print "Coding"

        else print i
    
*/